source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.6.10'

# Core Rails gems only - keep minimal for compatibility
gem 'rails', '~> 6.0.0'
gem 'sqlite3', '~> 1.3.13'

group :development, :test do
  gem 'rspec-rails', '~> 4.0'
end
